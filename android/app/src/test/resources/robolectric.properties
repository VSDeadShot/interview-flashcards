# Robolectric 4.15.1 ships no runtime image above API 35, and the app targets 36. Pinned here
# rather than lowering targetSdk, which would change what the app actually is to suit a test
# tool. Nothing these tests cover is version-sensitive — they exercise SQLite through Room, not
# framework behaviour that changed in 36. Raise this when Robolectric catches up.
sdk=35
