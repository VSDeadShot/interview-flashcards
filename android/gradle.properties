# AndroidX rather than the old support library. Required by every androidx dependency.
android.useAndroidX=true

# The build runs two toolchains' worth of annotation processing; the default 512m is not enough.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

org.gradle.caching=true
org.gradle.parallel=true
